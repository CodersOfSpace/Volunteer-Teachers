name: Volunteer_Teachers
description: Volunteer Teachers

version: 1.0.0+1

environment:
  sdk: '>=2.3.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  cloud_firestore:
  firebase_core : ^0.5.0
  firebase_auth:
  image_picker: ^0.6.5+3
  firebase_storage:
  firebase_messaging:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true


  assets:
    - assets/
